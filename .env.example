# این فایل را کپی کرده و به نام ".env" تغییر دهید
# سپس مقادیر متغیرها را بر اساس نیاز خود تنظیم کنید

#--------------------------#
# تنظیمات دیتابیس PostgreSQL
#--------------------------#
DB_HOST=postgres        # نام سرویس دیتابیس در docker-compose
DB_PORT=5432            # پورت دیتابیس
DB_USER=instabot        # نام کاربری دیتابیس
DB_PASSWORD=your_secure_password_here  # رمز عبور دیتابیس (حتماً تغییر دهید)
DB_NAME=instabot_db     # نام دیتابیس

#--------------------------#
# تنظیمات اینستاگرام
#--------------------------#
INSTA_USERNAME=your_instagram_username  # نام کاربری اینستاگرام
INSTA_PASSWORD=your_instagram_password  # رمز عبور اینستاگرام

#--------------------------#
# تنظیمات API و وب
#--------------------------#
API_PORT=8000           # پورت API
ADMINER_PORT=8080       # پورت Adminer برای مدیریت دیتابیس

#--------------------------#
# تنظیمات سیستمی
#--------------------------#
TIMEZONE=Asia/Tehran    # منطقه زمانی
LOG_LEVEL=INFO          # سطح لاگ (DEBUG, INFO, WARNING, ERROR, CRITICAL)

#--------------------------#
# تنظیمات رفتار بات (اختیاری)
#--------------------------#
# مقادیر پیش‌فرض در config.py تنظیم شده‌اند
# در صورت نیاز به تغییر می‌توانید موارد زیر را از حالت کامنت خارج کنید

# MAX_INTERACTIONS_PER_DAY=50   # حداکثر تعداد کل تعاملات روزانه
# MAX_FOLLOWS_PER_DAY=10        # حداکثر تعداد فالو روزانه
# MAX_UNFOLLOWS_PER_DAY=8       # حداکثر تعداد آنفالو روزانه
# MAX_COMMENTS_PER_DAY=5        # حداکثر تعداد کامنت روزانه
# MAX_DIRECT_MESSAGES_PER_DAY=3 # حداکثر تعداد پیام مستقیم روزانه
# MIN_DELAY_BETWEEN_ACTIONS=120  # حداقل تاخیر بین اقدامات (ثانیه)
# MAX_DELAY_BETWEEN_ACTIONS=600  # حداکثر تاخیر بین اقدامات (ثانیه)

#--------------------------#
# نکات امنیتی
#--------------------------#
# - از این فایل فقط برای تنظیمات استفاده کنید و آن را در مخزن گیت قرار ندهید
# - برای اطلاعات حساس مانند رمز عبور، از رمزهای قوی و منحصر به فرد استفاده کنید
# - اطمینان حاصل کنید که فایل .env در .gitignore قرار دارد